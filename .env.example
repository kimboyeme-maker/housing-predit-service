# Copy to .env to override defaults (all optional).
HPP_LOG_LEVEL=INFO
# Comma-separated allowed origins for CORS. Use your frontend origin in prod.
HPP_CORS_ORIGINS=*
# Point train.py at a different dataset if needed.
# HPP_DATASET=data/House Price Dataset.csv
